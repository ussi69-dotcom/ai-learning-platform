<Callout type="info">
**Mise:** Ovládněte AI-native vývojářský ekosystém. Posunete se za hranice prostého doplňování kódu k architektuře systémů, kde AI agenti interagují přímo s vaším prostředím prostřednictvím MCP.

⏳ **Čas čtení:** 35 min | 🧪 **[2] Laby součástí**
</Callout>

## 📹 Doporučené sledování

Než se ponoříme do kódu, podívejte se na tento rozbor toho, jak Model Context Protocol standardizuje způsob, jakým se AI modely propojují s daty.

*(Video Placeholder: https://youtube.com/watch?v=placeholder-mcp-explained)*

---

## Nový vývojářský stack

Éra "Stack Overflow driven development" končí. Vstupujeme do éry **Context-Aware AI Development**.

V minulosti byl vývojář mostem mezi problémem a syntaxí. Museli jste hledat knihovny, číst dokumentaci a ručně lepit kód dohromady. Dnes se stack vyvinul. AI není jen generátor textu; je to reasoning engine, který žije *uvnitř* vašeho prostředí.

### Evoluce workflow

1.  **Code Completion (2021-2022):** GitHub Copilot funguje jako chytré automatické doplňování. Předpovídá několik následujících řádků na základě aktuálního souboru.
2.  **Chat Integration (2023):** Kopírujete chyby do ChatGPT. Kontext je omezen na to, co vložíte.
3.  **Context-Aware IDEs (2024):** Cursor a Windsurf čtou celou vaši codebase (RAG). Rozumí struktuře projektu.
4.  **Agentic Architecture (2025):** MCP umožňuje modelům *dělat* věci—dotazovat se databází, kontrolovat statistiky systému a bezpečně interagovat s externími API.

<ConceptCard title="AI-Native vývojář">
Vývojář, který se zaměřuje na systémovou architekturu, správu kontextu a ověřování výstupů AI, spíše než na psaní syntaxe. Role se posouvá od "pisatele kódu" k "režisérovi AI agentů".
</ConceptCard>

---

## 🔌 Vysvětlení architektury MCP

**Model Context Protocol (MCP)** je pravděpodobně nejkritičtějším posunem v AI vývoji od vydání GPT-4. Vydaný jako otevřený standard, funguje jako "USB-C pro AI aplikace".

Před MCP vyžadovalo připojení LLM k databázi nebo GitHub repozitáři vlastní integrace pro každý jednotlivý nástroj. Pokud jste chtěli, aby Claude četl vaši Postgres DB, potřebovali jste specifický plugin. Pokud jste chtěli, aby to udělal ChatGPT, potřebovali jste jiný plugin.

MCP to řeší standardizovanou třídílnou architekturou:

### 1. Client (Hostitel)
Toto je aplikace, kde AI žije. Příklady zahrnují:
*   **Claude Desktop App:** Primární rozhraní pro mnohé.
*   **Cursor / Windsurf:** IDE, která fungují jako MCP klienti.
*   **Zed:** Vysoce výkonný editor.

Client je zodpovědný za udržování spojení a—což je zásadní—správu oprávnění. AI nemůže spustit nástroj, aniž by Client (a vy) řekl "Ano".

### 2. Server
Server je lehký program, který vystavuje specifické schopnosti (Resources, Prompts a Tools) Clientovi.
*   **Resources:** Pasivní data (např. čtení souboru, prohlížení logu).
*   **Tools:** Spustitelné funkce (např. `git commit`, `query_db`, `restart_server`).
*   **Prompts:** Předdefinované šablony pro interakci se serverem.

### 3. Model
Samotné LLM (Claude 3.5 Sonnet, GPT-4o). Model používá nástroje poskytované Serverem k zodpovězení požadavku uživatele.

### 🔑 Klíčové MCP servery (prosinec 2025)

Ekosystém exploduje. Zde jsou servery, které byste měli znát:

*   **GitHub MCP Server (29. listopadu):** Umožňuje AI prohledávat issues, prohlížet PR a číst historii souborů přímo.
*   **Microsoft 365 MCP Server (4. prosince):** Připojuje se k Teams, Outlook a OneDrive.
*   **Claude Desktop MCP:** Oficiální referenční implementace.
*   **YouTube MCP Server:** Získává titulky a metadata z videí v reálném čase.

<ConceptCard title="Proč na MCP záleží">
MCP eliminuje vendor lock-in. Nástroj, který vytvoříte pro Claude Desktop, bude fungovat s Cursor, Windsurf a jakýmkoli jiným MCP-kompatibilním clientem. Jedna integrace, nekonečné aplikace.
</ConceptCard>

---

## 🛠️ Srovnání AI nástrojů pro kódování

Trh s "AI IDE" se fragmentoval do specializovaných nástrojů. Výběr toho správného závisí na vašem konkrétním workflow—zda potřebujete hluboký refactoring, rychlé prototypování nebo každodenní údržbu.

Stav trhu k prosinci 2025:

| IDE | Kódování | Agent | Cena | Nejlepší pro |
|-----|:--------:|:-----:|:----:|--------------|
| **Cursor** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | $20/měsíc | **Daily driver.** Nejlepší UX pro editaci kódu, "Tab" pro autocomplete, solidní RAG integrace. |
| **Windsurf** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | $10/měsíc | **Agentic flows.** Jeho "Cascade" flow umožňuje plánovat vícekrokové změny lépe než Cursor. |
| **Claude Code** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | $20/měsíc | **Refactoring.** Žije v terminálu. Výjimečný v pochopení velkých architektonických změn. |
| **Antigravity** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | **ZDARMA** | **Prototypování.** Python-native plátno pro rychlé budování aplikací od nuly. |

### Hloubkový pohled: Cursor vs. Windsurf

**Cursor** vyhrává na rychlost. Jeho "shadow workspace" předpovídá váš další edit ještě předtím, než ho uděláte. Působí to jako telepatie.

**Windsurf** (od Codeium) vyhrává na *hloubku*. Buduje hluboké porozumění toku proměnných ve vaší aplikaci. Pokud potřebujete "Aktualizovat objekt uživatele napříč frontendem, backendem a databází," Windsurf často zvládá propagaci kontextu lépe.

<Callout type="tip">
**Pro Tip:** Mnoho vývojářů používá oba. Cursor pro rychlé úpravy a refactoring jednotlivých souborů. Windsurf když potřebujete "hlubokou chirurgii" napříč více propojenými moduly.
</Callout>

---

## 🧠 Správa kontextu

Pokud je LLM motor, **Kontext** je palivo.

Největší chyba, kterou vývojáři dělají, je očekávat, že AI "prostě bude vědět" jejich styl kódování. Musíte explicitně definovat "Ústavu" svého projektu.

### Soubor `.cursorrules` / `.windsurfrules`

Tyto soubory žijí v kořenovém adresáři vašeho repozitáře. Fungují jako system prompt, který se vkládá do každé chatové relace.

**Příklad: Robustní kontextový soubor**

```markdown
# Project Context
Stack: Next.js 15 (App Router), Tailwind CSS, Supabase, TypeScript.

# Coding Standards
- Use functional components only.
- Prefer `const` over `let`.
- ALL asynchronous code must handle errors with try/catch.
- UI Components: Use Shadcn UI located in `@/components/ui`.

# Testing
- Write tests using Playwright for E2E.
- Mock all external API calls in unit tests.

# DO NOT
- Never use `any` type in TypeScript.
- Never commit secrets or API keys.
```

Bez tohoto souboru by AI mohla navrhovat React Class komponenty nebo vanilla CSS. S ním se AI stává senior inženýrem ve vašem specifickém týmu.

---

## 🧪 Lab 1: Vytvoření vlastního MCP serveru s FastMCP

V tomto labu vytvoříme "System Monitor" MCP server. To umožní vašemu AI asistentovi (např. Claude Desktop) vidět stav vašeho počítače v reálném čase.

### 🎯 Cíl
Vytvořit Python-based MCP server, který vystavuje využití CPU a RAM jako nástroj.

### 📋 Prerekvizity
- Python 3.10+ nainstalovaný.
- Claude Desktop App nainstalovaná.
- Základní znalost terminálu.

### 🛠️ Kroky

#### Krok 1: Instalace závislostí
Použijeme `fastmcp`, high-level knihovnu, která činí budování serverů triviálním.

```bash
pip install mcp[cli] psutil
```

#### Krok 2: Napsání kódu serveru
Vytvořte soubor s názvem `monitor_server.py`.

```python
from mcp.server.fastmcp import FastMCP
import psutil

# Inicializace Serveru
mcp = FastMCP("SystemMonitor")

@mcp.tool()
def get_system_stats() -> str:
    """Returns current CPU and RAM usage.
    Use this tool when the user asks about computer performance or lag."""

    cpu = psutil.cpu_percent(interval=1)
    ram = psutil.virtual_memory().percent

    return f"CPU: {cpu}%\nRAM: {ram}%"

if __name__ == "__main__":
    mcp.run()
```

#### Krok 3: Konfigurace Claude Desktop
Musíte říct Claude, kde najít tento server.

1. Otevřete konfigurační soubor Claude Desktop:
   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. Přidejte konfiguraci vašeho serveru:

```json
{
  "mcpServers": {
    "system-monitor": {
      "command": "python",
      "args": ["/absolutni/cesta/k/monitor_server.py"]
    }
  }
}
```

#### Krok 4: Ověření
1. Restartujte Claude Desktop.
2. Hledejte ikonu "zástrčky" (🔌) indikující připojené servery.
3. Zeptejte se Claude: *"Jak si vede můj systém právě teď?"*
4. **Pozorování:** Claude by měl spustit nástroj `get_system_stats` a odpovědět s vašimi real-time daty CPU/RAM.

### ✅ Kritéria úspěchu
- Server se spustí bez chyb.
- Claude Desktop rozpozná server `system-monitor`.
- Nástroj `get_system_stats` je úspěšně zavolán a vrací živá data.

---

## 🧪 Lab 2: Srovnání AI IDE

Abyste skutečně pochopili "agentický" rozdíl, provedeme A/B test napříč různými AI nástroji pro kódování.

### 🎯 Cíl
Implementovat stejnou funkci pomocí dvou různých AI IDE a porovnat jejich přístupy.

### 📋 Prerekvizity
- Přístup k Cursor a jednomu dalšímu AI nástroji (Windsurf nebo Claude Code).
- Zkušební React/Node projekt.

### 🛠️ Kroky

#### Definice úkolu
**Úkol:** "Vytvoř responzivní navigační lištu s přepínačem dark mode. Použij Tailwind CSS. Přepínač by měl persistovat preferenci do localStorage."

#### Kolo 1: Cursor (Composer Mode)
1. Otevřete Composer (`Cmd+I` nebo `Ctrl+I`).
2. Vložte prompt.
3. Pozorujte: Vytvoří více souborů? Upraví správně `layout.tsx`?
4. **Hodnocení:** Zaznamenejte rychlost a přesnost vygenerovaného UI.

#### Kolo 2: Windsurf (Cascade Flow)
1. Vraťte změny (`git reset --hard HEAD`).
2. Otevřete Windsurf Cascade.
3. Vložte *přesně stejný prompt*.
4. **Pozorování:** Všimněte si, zda Windsurf analyzuje váš existující `tailwind.config.js` *před* generováním kódu. Ptá se na upřesňující otázky ohledně vaší knihovny ikon?

#### Kolo 3 (Volitelné): Claude Code
1. Vraťte změny znovu.
2. V terminálu spusťte `claude` a vložte prompt.
3. **Pozorování:** Claude Code typicky vysvětluje svůj plán před provedením. Exceluje u multi-file refactorů.

### ✅ Kritéria úspěchu
- Identifikovali jste, který nástroj lépe odpovídá vašemu mentálnímu modelu.
- Obecně: Cursor je rychlejší pro "fire and forget," Windsurf je více deliberativní ohledně struktury projektu, Claude Code vysvětluje reasoning.

---

## 🛡️ Bezpečnostní best practices

Dát AI přístup k vašemu shellu, databázi a souborovému systému je ze své podstaty riskantní. Jak se posouváme od Chatu k Agentům, bezpečnost musí být proaktivní.

### 1. Princip nejmenších oprávnění (OAuth Scopes)
Při připojování MCP serveru (jako GitHub nebo Google Drive) **nikdy** neudělujte plný read/write přístup, pokud není potřeba.
*   **Špatně:** Scope `repo` (Plná kontrola privátních repozitářů).
*   **Dobře:** Scope `read:issue`, `read:pr`.

### 2. Container Sandboxing
Spouštějte MCP servery v izolovaných prostředích. Pokud stáhnete komunitní MCP server, nespouštějte ho přímo na vašem host stroji. Spusťte ho uvnitř Docker kontejneru.

```dockerfile
# Příklad: Spuštění nedůvěryhodného MCP serveru v Dockeru
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
# Vystavte pouze stdio, žádnou síť pokud není vyžadována
CMD ["python", "server.py"]
```

### 3. Audit Logging
Musíte vědět, co AI udělala.
*   Povolte logování spuštění ve vašem Clientovi (Cursor/Claude).
*   Kontrolujte logy: *Pokoušela se AI číst `.env` soubory? Pokoušela se exfiltrovat data?*

### 4. Human-in-the-Loop (HITL)
Kritické operace—`DELETE`, `DROP TABLE`, `git push --force`—by **vždy** měly vyžadovat explicitní potvrzení uživatelem. Nakonfigurujte vaše MCP servery, aby označovaly vysoce rizikové nástroje jako "requires_approval."

<Callout type="warning">
**Bezpečnostní varování:** Nikdy nespouštějte MCP servery z nedůvěryhodných zdrojů bez code review. Škodlivý server by mohl ukrást credentials, číst citlivé soubory nebo spouštět libovolný kód na vašem stroji.
</Callout>

---

## 🔮 Holocron: Klíčové poznatky

<ConceptCard title="Architektura AI-Powered Development">
1. **MCP je nový standard.** Poskytuje infrastrukturu pro komunikaci AI s nástroji, zabraňuje vendor lock-in.
2. **Nástroje se specializují.** Použijte **Cursor** pro rychlost, **Windsurf** pro context-aware agenty, **Claude Code** pro architekturu.
3. **Kontext je král.** Váš `.cursorrules` soubor je stejně důležitý jako váš zdrojový kód. Definuje úroveň inteligence vašeho agenta.
4. **Bezpečnost je nové úzké hrdlo.** Zacházejte s AI agenty jako s junior vývojáři: dejte jim nástroje, ale omezte jejich oprávnění a auditujte jejich práci.
</ConceptCard>
